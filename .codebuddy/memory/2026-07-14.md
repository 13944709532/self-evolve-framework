# 2026-07-14 工作日志

## sync-docs 技能进化 + 项目文档全量对齐

从 `D:\code\tauri\.codebuddy\skills\sync-docs\SKILL.md` 读取原始 sync-docs 技能后，进行了以下工作：

### sync-docs 技能进化（v2）
- **原始 v1**：硬编码 6 个文档文件，仅支持 Tauri 项目（检查 src-tauri/src/）
- **进化 v2**：自动检测项目类型（框架模板 / Tauri 应用 / Vue-Svelte 前端 / 通用），选择对应文档集和检查策略
- 创建了两个版本：
  - `.codebuddy/skills/sync-docs/SKILL.md` — 框架项目专用版（自动检测 + 模板内容对比）
  - `template/skills/sync-docs/SKILL.md` — 通用于目标 Tauri/Svelte 项目的分发版

### 项目文档对齐（5 个文件）

1. **README.md** — 4 处修正：
   - "安装了什么"：移除不存在的 ponytail 系列 6 个 skill，新增 sync-docs
   - "按需检查"：移除 ponytail 命令，新增 sync-docs
   - "文件结构"：模板 skills 列表与实际对齐，新增 AGENTS.md + LICENSE
2. **AGENTS.md** — 新建，含仓库结构、构建命令、模板内容表、关键约束、自定义指令
3. **CLAUDE.md** — 新建，含项目身份、硬性约束、模块速览、文档对齐规则
4. **.gitignore** — 修正 `"*.txt"`（引号包裹导致不匹配）→ `*.txt`
5. **template/skills/sync-docs/** — 新建 skill 目录 + SKILL.md

### 三者互通关系
- README.md ← 用户文档源头
- AGENTS.md ← AI 代理可读的项目指南 + 指令映射
- CLAUDE.md ← AI 代理的硬性约束
- 三者通过 sync-docs skill 保证一致性

## Rust 项目 .gitignore 自动补充

sync-docs 新增规则：检测到 `Cargo.toml` 或 `src-tauri/` 时，自动检查 `.gitignore` 是否含 `Cargo.lock`，缺失则追加。同时执行 `git rm --cached Cargo.lock` 移除已有的 Git 跟踪。
更新了两个版本：`template/skills/sync-docs/SKILL.md`（分发版）+ `.codebuddy/skills/sync-docs/SKILL.md`（框架专用版）。

## 跨项目记忆同步 — 规则批量纳入模板

从 `D:\code\tauri` 和 `D:\code\sheetflow` 的多轮问题解决经验中提取规则，纳入框架模板：

### 模板结构重构
`template/rules/` 改为按技术栈分目录：
```
template/rules/
├── always/    ← 7 个通用规则（始终安装）
├── rust/      ← 5 个 Rust/Tauri 规则（检测到 Rust 时安装）
├── svelte/    ← 2 个 Svelte 规则（检测到 Svelte 时安装）
└── tailwind/  ← 1 个 Tailwind 规则（检测到时安装）
```

### 新增规则（13 个）

**通用规则（5 个新增，来自 tauri）：**
- `windows-cmd.mdc` — Unix→CMD 命令等价替代表
- `powershell.mdc` — Get-Content -Encoding UTF8
- `error-message-exposure.mdc` — 错误消息强制友好化（★★★★ 高频重复模式）
- `verify-done.mdc` — 标记完成前必须核实代码（≥2 次谎报）
- `CodeGraph.mdc` — MCP 工具选择指南（来自 sheetflow）

**Rust/Tauri 规则（5 个，来自 tauri + sheetflow）：**
- `db-path.mdc` — Tauri 2 DB 路径必须绝对路径
- `rust-type-safety.mdc` — SQLite get_unwrap→Value 枚举
- `app-error-pattern.mdc` — AppError 6 变体（sheetflow）
- `invoke-safe-pattern.mdc` — invokeSafe + INVOKE_ERROR（sheetflow）
- `Tauri.mdc` — Tauri v2 官方规范（sheetflow）

**Svelte 规则（2 个，来自 sheetflow）：**
- `Svelte_5.mdc` — $state/$derived/$props 100% 规范
- `Svelte_Flow.mdc` — @xyflow/svelte 节点图规范

**Tailwind 规则（1 个，来自 sheetflow）：**
- `Tailwind_CSS_v4.mdc` — @import/v4 规范

### CLI 智能安装（`bin/cli.js` 重写）
- `detectTechStack()` — 读取 package.json deps + 检测 Cargo.toml/src-tauri/svelte.config/tailwind.config
- `copyRulesFlat()` — 递归复制子目录中的 .mdc 到目标 .codebuddy/rules/（扁平化）
- `listSkills()` — 按分类展示所有规则和技能
- dry-run 模式下打印完整预览

## Git 合并冲突解决（远端 knowledge/ 结构采用）

- 规则目录由 `always/rust/svelte/tailwind/` 切换为远端 `knowledge/`（9 个经验库）+ 顶层 `self-evolve.mdc` + `ponytail.mdc`
- `package.json` 采用远端 v1.6.1
- `README.md` / `AGENTS.md` / `CLAUDE.md` 全部对齐至 knowledge/ 结构
- CLI 简化为扁平复制（无需技术栈检测，知识库文件通过 alwaysApply 自行控制）
- skillopt_sleep 路径修复保留：`scripts/python/`→`skillopt_sleep/`，`types.py`→`_types.py`
- `.gitignore` 新增 `__pycache__/`
- 合并后 11 规则 + 3 技能，CLI `list`/`init` 验证通过
