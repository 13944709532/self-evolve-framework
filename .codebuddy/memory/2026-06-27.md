## 2026-06-27

### fix: /skillopt 命令误导 + npm v1.0.5 发布

- 修复 4 个文件中的 `/skillopt` 命令引用（bin/cli.js、README.md、template/rules/self-evolve.mdc、template/skills/skillopt-sleep/SKILL.md），改为 `skillopt-sleep` 前缀并说明是对话触发器而非 CLI 命令
- bump v1.0.4 → v1.0.5
- git commit + push + tag v1.0.5
- npm publish 到 registry.npmjs.org（7 个文件，15.7 kB）

### feat: 添加"核心边界审计"到模板

受用户启发"不要向能丢什么进化，要向不能丢什么思考"，扩展两个模板文件：
- `template/rules/self-evolve.mdc`: 调度架构新增"核心边界审计"条目，前置检查新增删功能代码的门控步骤，步骤 2 新增核心边界约束说明，步骤 4 新增核心路径修改的记录规则，新增独立"核心边界审计规则"章节
- `template/skills/skillopt-sleep/SKILL.md`: Mine 阶段新增"核心边界"识别项，Consolidate 阶段新增"边界标记"（⚠️ 要求人工确认）
- 两次版本变更归并后统一发布 v1.0.6

### ⏳ 待办：GitHub 补推

- self-evolve-framework v1.0.6 的 commit（bf8edc4）已在本地 master，未推远程
- 未打 tag v1.0.6
- 网络恢复后执行：
  ```bash
  cd d:\code\self-evolve-framework
  git push origin master
  git tag v1.0.6
  git push origin v1.0.6
  ```
